
Дана одна таблица с работниками, у каждого работника есть мененджер(тоже является работником), нужны работники у которых зарплата больше чем у мененджера

select * from emp e inner join emp a on a.empno = e.mgr where e.sal > a.sal;
